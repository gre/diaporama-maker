
.library {
  .body {
    overflow: auto;
    padding: 1px 5px;
  }
  .item {
    margin: 0 2px;
    display: inline-block;
    .name {
      font-size: 0.8em;
      font-weight: 300;
      color: #666;
      display: inline-block;
      overflow: ellipsis;
      width: 100%;
    }
  }
}
